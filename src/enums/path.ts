export enum path {
  LOGIN = '/login',
  PROFILE = '/profile',
  USERS = '/users',
  ANOTHER_PROFILE = '/profile/:userId',
  SETTINGS = '/settings',
  MESSAGES = '/messages/*',
  DIALOGS = '/dialogs',
  CHAT = '/chat',
  PAGE_NOT_FOUND = '*',
}
