export enum path {
  LOGIN = '/login',
  PROFILE = '/profile',
  ANOTHER_PROFILE = '/profile/:userId',
  USERS = '/users',
  SETTINGS = '/settings',
  MESSAGES = '/messages/*',
  DIALOGS = '/dialogs',
  CHAT = '/chat',
  PAGE_NOT_FOUND = '*',
}
