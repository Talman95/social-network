export enum path {
    LOGIN = '/login',
    PROFILE = '/profile',
    USERS = '/users',
    ANOTHER_PROFILE = '/profile/:userId',
    SETTINGS = '/settings',
    MESSAGES = '/messages/*',
    CHAT = '/chat',
    PAGE_NOT_FOUND = '*',
}